{
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0"
  },
  "scripts": {
    "build": "babel js/src/*.es6 --out-file js/script.js --source-maps",
    "watch": "babel js/src/*.es6 --out-file js/script.js --source-maps --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
